name: Build Android APK (Cordova)

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Install Cordova
      run: npm install -g cordova

    - name: Create Cordova Project
      run: |
        cordova create myapp com.anestesi.kalkulator "Kalkulator Anestesi"
        cd myapp
        cordova platform add android
        
    - name: Copy HTML Files to Cordova www
      run: |
        rm -rf myapp/www/*
        cp -r * myapp/www/ || true
        # Menangani toleransi nama file utama di dalam aplikasi
        if [ -f "myapp/www/in.html" ]; then cp myapp/www/in.html myapp/www/index.html; fi

    - name: Setup Java JDK
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Build APK
      run: |
        cd myapp
        cordova build android --device

    - name: Upload APK Release Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Kalkulator-Anestesi-APK
        path: myapp/platforms/android/app/build/outputs/apk/debug/app-debug.apk
